afficher_objet(objet) -> afficher sur la map
verification_niveau() -> True si le perso est sur un niveau dans la map (les mêmes coordonnées que l'objet "niveau")
afficher_niveau(pos)
changer_resolution(resolution)
changer_FPS(FPS)
changer_musique(status_musique) -> couper/remettre musique
creer_bouton(image, texte, taille, pox_x, pos_y)

------------------------------------

avancer_jeu(vitesse, sens) -> sens : droite/gauche. Pas la même variable que "avancer". Celle-là c'est pour quand on est dans un niveau
sauter()
attaquer()
verification_distance() -> renvoie distance entre joueur et ennemi
mettre_degat_boss(nb_degat)
mettre_degat_joueur(nb_degat)
boss_attaquer()
animation_attaque()


Quand le personnage arrive près de l'eau -> son de l'eau
